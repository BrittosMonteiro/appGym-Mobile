import Header from '../../components/Header';
import ViewDefault from '../ViewDefault';

export default function ActivityCreate({navigation}) {
  return (
    <ViewDefault>
      <Header navigation={navigation} title={'CRIAR TREINO'} />
    </ViewDefault>
  );
}
